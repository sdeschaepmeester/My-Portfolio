import React from 'react';

export default function NavBar() {
    return (
        <nav className="w-full h-16 bg-black text-white flex items-center fixed top-0 left-0 z-50 opacity-50">
            <h1 className="text-xl font-bold">Mon Portfolio</h1>
        </nav>
    );
}
